var user_generics  = new function(){console.log('Dynamic user_generics forms js loaded');document.addEventListener('DOMContentLoaded',function(){FormFunctions.tidyMultiInputs();let forms=document.querySelectorAll(`[data-formid="10"]`);forms.forEach(form=>{form.querySelectorAll(`select, input, textarea`).forEach(el=>user_generics.p(el));});});var prevEl='';var q=function(event){var el=event.target;form=el.closest('form');var n=el.getAttribute('name');if(n==''||n==undefined){if(el.closest('.nice-select-dropdown')!=null&&el.closest('.inputwrapper')!=null){el.closest('.inputwrapper').querySelectorAll('select').forEach(select=>{if(el.dataset.value==select.value){el=select;n=select.name;}});}else{return;}}if(el==prevEl){return;}prevEl=el;setTimeout(function(){prevEl='';},50);if(n=='nextBtn'){FormFunctions.nextPrev(1);}else if(n=='prevBtn'){FormFunctions.nextPrev(-1);}user_generics.p(el);};window.addEventListener('click',q);window.addEventListener('input',q);this.p=function(el){var n=el.getAttribute('name');var form=el.closest('form');if(n=='nickname'){let nickname=FormFunctions.getFieldValue('nickname',form);if(n=='nickname'){FormFunctions.changeFieldValue('[name="first_name_label"]',nickname,user_generics.p,form,);FormFunctions.changeFieldValue('[name="display_name"]',nickname,user_generics.p,form,);}}if(n=='account-type'){var v_1=FormFunctions.getFieldValue('account-type',form,true,'positional',true);if(v_1=='positional'){form.querySelectorAll(`[name="Sending office"], [name="secondary_e-mail_address_label"], [name="email"], [name="sending_office"], [name="arrival_date_in_nigeria_label"]`).forEach(el=>{if(!el.closest('.inputwrapper').matches('.action-processed')){el.closest('.inputwrapper').classList.add('action-processed');FormFunctions.changeVisibility('add',el,user_generics.p);}});document.querySelectorAll('.action-processed').forEach(el=>{el.classList.remove('action-processed')});FormFunctions.changeVisibility('remove',form.querySelector('[name="date_of_joining_sim_label"]').closest('.inputwrapper'),user_generics.p);}if(v_1!='positional'){form.querySelectorAll(`[name="Sending office"], [name="secondary_e-mail_address_label"], [name="email"], [name="sending_office"], [name="arrival_date_in_nigeria_label"]`).forEach(el=>{if(!el.closest('.inputwrapper').matches('.action-processed')){el.closest('.inputwrapper').classList.add('action-processed');FormFunctions.changeVisibility('remove',el,user_generics.p);}});document.querySelectorAll('.action-processed').forEach(el=>{el.classList.remove('action-processed')});FormFunctions.changeVisibility('add',form.querySelector('[name="date_of_joining_sim_label"]').closest('.inputwrapper'),user_generics.p);}}if(n=='financial_account_id'){var v_1=FormFunctions.getFieldValue('financial_account_id',form,true,'',true);if(v_1!=''){form.querySelectorAll(`[name="Financial statements"], [name="info_finance"], [name="financial_account_id"], [name="info_download"], [id^='E151']`).forEach(el=>{if(!el.closest('.inputwrapper').matches('.action-processed')){el.closest('.inputwrapper').classList.add('action-processed');FormFunctions.changeVisibility('remove',el,user_generics.p);}});document.querySelectorAll('.action-processed').forEach(el=>{el.classList.remove('action-processed')});}if(v_1==''){form.querySelectorAll(`[name="Financial statements"], [name="info_finance"], [name="financial_account_id"], [name="info_download"], [id^='E151']`).forEach(el=>{if(!el.closest('.inputwrapper').matches('.action-processed')){el.closest('.inputwrapper').classList.add('action-processed');FormFunctions.changeVisibility('add',el,user_generics.p);}});document.querySelectorAll('.action-processed').forEach(el=>{el.classList.remove('action-processed')});}}if(n=='advanced_options_button'){if(n=='advanced_options_button'){form.querySelectorAll(`[name="privacy_preferences_label"], [name="privacy_explain"], [name="privacy_preference[]"], [name="nickname_label"], [name="nickname"]`).forEach(el=>{if(!el.closest('.inputwrapper').matches('.action-processed')){el.closest('.inputwrapper').classList.add('action-processed');FormFunctions.changeVisibility('toggle',el,user_generics.p);}});document.querySelectorAll('.action-processed').forEach(el=>{el.classList.remove('action-processed')});}}if(n=='age_preference[]'){if(el.checked){FormFunctions.changeFieldValue('[name="privacy_preference[]"]',"hide_age",user_generics.p,form,);}}};};if(typeof(urlSearchParams)=='undefined'){window.urlSearchParams=new URLSearchParams(window.location.search.replaceAll('&amp;','&'));}Array.from(urlSearchParams).forEach(array=>document.querySelectorAll(`[name^='${array[0]}']`).forEach(el=>FormFunctions.changeFieldValue(el,array[1],user_generics.p,el.closest('form'),)));

//Show the position field when a ministry is checked
function changeVisibility(target) {
	target.closest('li').querySelectorAll('.ministryposition').forEach(label=>{
		label.classList.toggle('hidden');
		label.querySelectorAll('input').forEach(el=>el.value = '');
	});
}

async function addNewMinistry(target){
	var response = await FormSubmit.submitForm(target, 'user_management/add_ministry');
	
	if(response){
		var ministryName 		= target.closest('form').querySelector('[name="location_name"]').value;
		ministryName			= ministryName.charAt(0).toUpperCase() + ministryName.slice(1);

		var html = `
		<li style="list-style-type: none"> 
			<label>
				<input type="checkbox" class="ministry_option_checkbox" name="ministries[]" value="${response.postId}" checked>
				<span class="optionlabel">${ministryName}</span>
			</label>
			<label class="ministryposition" style="display:block;">
				<h4 class="labeltext">Position at ${ministryName}:</h4>
				<input type="text" id="justadded" name="jobs[${response.postId}]">
			</label>
		</li>`;
		
		document.querySelector("#ministries_list").insertAdjacentHTML('beforeEnd', html);
		
		//hide the SWAL window
		setTimeout(function(){document.querySelectorAll('.swal2-container').forEach(el=>el.remove());}, 1500);

		//focus on the newly added input
		document.getElementById('justadded').focus();
		document.getElementById('justadded').select();

		Main.displayMessage(response.html)
	}
	
	Main.hideModals();
}

//listen to all clicks
document.addEventListener('click', function(event) {
	var target = event.target;
	//show add ministry modal
	if(target.id == 'add-ministry-button'){
		//uncheck other and hide
		target.closest('li').querySelector('.ministry_option_checkbox').checked = false;
		target.closest('.ministryposition').classList.add('hidden');

		//Show the modal
		Main.showModal('add_ministry');
	}
	
	if(target.matches('.ministry_option_checkbox')){
		changeVisibility(target);
	}

	if(target.name == 'add_ministry'){
		addNewMinistry(target);
	}
});

function onBlur(ev){
	document.querySelectorAll(`.ministryposition input[type="text"][name="${ev.target.name}"]`).forEach(input => {
		// set value
		input.value = ev.target.value;

		// make visible
		input.closest('.ministryposition').classList.remove('hidden');

		// check the ministry
		input.closest('li').querySelector('.ministry_option_checkbox').checked	= true;
	});
}

document.addEventListener("DOMContentLoaded", function() {
	// Add the value to all inputs with the same name
	document.querySelectorAll('.ministryposition input[type="text"]').forEach(el => {
		el.addEventListener('blur', onBlur);
	});
});